<div class="modal fade" id="reset_password" tabindex="-1" role="dialog">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
        <h3 class="modal-title">パスワード再設定</h3>
      </div>
      <div class="modal-body">
        <p>以下にご入力のアドレスに、パスワード再設定用のメールを送信いたします。ご確認の上、再設定をお願いいたします。</p>
        <%= form_for Spree::User.new, as: :spree_user, url: potepan_reset_password_path do |f| %>
          <div class="form-group">
            <%= f.label :email, "メールアドレス" %>
            <%= f.email_field :email, class: "form-control" %>
          </div>
          <%= f.submit "送信", class: "btn btn-primary btn-block" %>
        <% end %>
      </div>
    </div>
  </div>
</div>
