<% provide(:title, "#{@taxon.name} - BIGBAG Store") %>
<div class="main-wrapper">
  <%= render "light_section", taxon: @taxon %>
  <!-- MAIN CONTENT SECTION -->
  <section class="mainContent clearfix productsContent">
    <div class="container">
      <div class="row">
        <%= render "side_bar", taxonomies: @taxonomies %>
        <%= render "main_panel", products: @taxon.all_products %>
      </div>
    </div>
  </section>
</div>
<%= render "potepan/shared/login_modal" %>
<%= render "potepan/shared/signup_modal" %>
